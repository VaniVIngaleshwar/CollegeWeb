import { AttendanceDirective } from './attendance.directive';

describe('AttendanceDirective', () => {
  it('should create an instance', () => {
    const directive = new AttendanceDirective();
    expect(directive).toBeTruthy();
  });
});
